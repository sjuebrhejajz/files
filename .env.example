# Database (Neon / Vercel Postgres) — run lib/schema.sql against this once
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# Signs short-lived login-2FA tickets. Generate with: openssl rand -hex 32
AUTH_SECRET=

# Resend (verification / reset / login-code emails)
RESEND_API_KEY=
RESEND_FROM_EMAIL=onboarding@resend.dev

# Twilio (SMS for phone-based 2FA)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Stripe (already in use by /api/donate)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# S3 / R2 (for profile picture uploads — reuse whatever your file uploader already uses)
S3_ENDPOINT=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET=
S3_PUBLIC_BASE_URL=
